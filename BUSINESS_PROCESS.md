# StableEra 业务流程文档

> 本文档描述 StableEra 相册系统的业务流程、权限边界与数据模型，包含「照片上传默认为公开」的本次变更说明。
>
> 更新日期：本次「照片公开化」改造完成时点。

---

## 1. 项目概述

StableEra 是一个自托管的相册系统（基于 Next.js + Hono 二次开发），支持多用户、多存储（本地 / S3 兼容 / Vercel Blob），以瀑布流方式浏览照片并解析 EXIF 元数据。

**本次业务调整核心**：

| 项目 | 调整前 | 调整后 |
|------|--------|--------|
| 照片可见范围 | 每个用户只能看到自己上传的照片 | 照片墙展示**所有用户**上传的公开照片（照片墙、日期统计、收藏页均可见） |
| 上传提示 | 无可见性提示 | 上传弹窗顶部展示醒目的「上传后默认为公开」提示 |
| 照片文件访问 | 仅本人可读取媒体文件 | 所有登录用户均可查看任意照片（原图/预览/缩略图） |
| 照片管理权限 | —（只能管理自己的照片） | 仍只允许照片所有者进行删除/回收/加入相册等操作；非本人照片不显示管理按钮 |

---

## 2. 技术架构与分层

```
┌────────────────────────────────────────────────────────────┐
│  前端（Next.js App Router + React 19 + shadcn/ui）          │
│  ├─ 页面层：照片墙 / 收藏 / 相册 / 回收站 / 用户 / 存储 / 设置│
│  ├─ 组件层：瀑布流、上传弹窗、查看器、选择抽屉等             │
│  └─ 状态层：zustand 全局 store + 服务端预取 Provider         │
├────────────────────────────────────────────────────────────┤
│  API 层（Hono，/api/[...route]）                            │
│  └─ 业务接口：photo / album / user / storage / setting / ...│
├────────────────────────────────────────────────────────────┤
│  Service 层（MVC 的 Controller 逻辑）                       │
│  └─ photo-service / album-service / user-service / ...      │
├────────────────────────────────────────────────────────────┤
│  数据层（Drizzle ORM + SQLite / Turso）                     │
│  └─ photo / file / album / album_photo / user / storage / ...│
└────────────────────────────────────────────────────────────┘
```

媒体访问走独立路由 `/media/{key}`（Hono `media.ts`），登录后按文件 key 直接读取存储中的原图、预览图与缩略图。

---

## 3. 角色与权限模型

| 角色 | 说明 | 权限范围 |
|------|------|----------|
| 管理员（admin） | 系统管理角色 | 用户管理（增删改）、存储管理、系统设置，以及普通用户的全部功能 |
| 普通用户（user） | 相册使用者 | 上传照片、浏览照片墙、管理自己的照片与相册、收藏等 |
| 演示用户（demo） | 只读体验角色 | 可浏览全部公开照片，**禁止**上传与修改类操作（前端拦截 + 后端接口仍受登录保护） |

**照片墙公开化后的权限边界**：

- **可见**：所有登录用户可见照片墙上的全部公开照片（含他人照片）及其原图/预览/缩略图。
- **管理**：只有照片所有者能对照片执行回收、恢复、彻底删除、加入/移出相册、收藏操作（后端按 `userId` 过滤，前端对非本人照片隐藏管理按钮）。
- **相册**：相册是个人数据，每个用户只能看到和管理自己的相册；相册内照片仅限本人照片。

---

## 4. 核心业务流程

### 4.1 登录认证流程

```
用户打开登录页 → 输入用户名/密码/验证码
        │
        ▼
登录接口（/login）校验验证码与账号密码（服务端对密码加盐哈希比对）
        │ 成功
        ▼
签发 JWT（写入 HttpOnly Cookie）→ 跳转照片墙首页
        │
        ▼
后续所有 /api 与 /media 请求携带 Cookie，由 security 中间件解析出当前用户 ID
```

- 未登录访问受保护页面时，服务端渲染层返回 null 或跳转登录页。
- 媒体路由要求登录：`getUserId()` 为空时直接拒绝返回文件。

### 4.2 照片上传流程（默认公开）

```
照片墙 / 相册页点击「上传照片」→ 打开上传弹窗
        │
        ▼
弹窗顶部醒目提示：“上传后照片默认为公开：所有登录用户都能看到你上传的照片。”（中/英文）
        │
        ▼
选择照片 → 生成预览封面 → 选择存储配置 → 点击「开始」
        │
        ▼
[去重检查] 计算文件 SHA-1 → 调用 /photo/exists
        │ 未重复
        ▼
[上传] 普通上传：FormData → /photo/add
       直传（Vercel/Blob）：先 /photo/createUrl 拿预签名 URL → PUT 直传 → 再调 /photo/add 登记
        │
        ▼
后端处理（/photo/add）：
  1. 读取文件字节 → 生成 preview（JPEG）、thumbnail（WebP）、thumbHash
  2. 解析 EXIF（拍摄时间、GPS、相机参数）
  3. 写入 photo 记录（userId = 当前用户，status = 正常）
  4. 写入 file 记录（原图 / 预览 / 缩略图三条）
  5. 若指定了相册，同时建立 album_photo 关联
        │
        ▼
返回照片对象 → 前端按拍摄时间插入照片墙列表
```

**关键点（本次变更）**：上传成功的照片不设任何私有属性，`userId` 只用于标识所有者，不再作为可见性隔离条件。照片墙查询不再按 `userId` 过滤，因此**所有用户上传的照片对其他用户立即可见**。唯一例外：照片一旦加入**任意用户**的「私密相册」，会从照片墙隐藏（见 4.5）。

### 4.3 照片墙浏览流程

```
进入「照片」页（服务端预取第一页）
        │
        ▼
调用 /photo/list（status=正常，不再过滤 userId）
  ├─ 排除任意用户私密相册中的照片（子查询 notIn）
  ├─ 支持关键词搜索（文件名模糊）、拍摄时间范围筛选、收藏筛选
  ├─ 游标分页（按 takenTime + photoId 倒序，每页 95 张）
        │
        ▼
瀑布流渲染（虚拟滚动 + 缩略图懒加载 + 触底加载下一页）
        │
        ▼
点击照片 → 打开查看器（支持上一张/下一张、原图加载、全屏、旋转、EXIF 信息侧栏）
```

- 日期抽屉：调用 /photo/takenDateList，按前端时区把拍摄日期分组统计，作为时间筛选入口。
- 搜索结果与时间筛选叠加，300ms 防抖后刷新第一页。

### 4.4 照片管理流程（仅限本人照片）

| 操作 | 接口 | 说明 |
|------|------|------|
| 收藏/取消收藏 | /photo/favorite | 单张：卡片红心按钮；仅本人照片显示红心 |
| 移入回收站 | /photo/recycle | 照片墙批量操作，标记 status=删除并记录回收时间 |
| 从回收站恢复 | /photo/restore | 恢复 status=正常，清空回收时间 |
| 彻底删除 | /photo/delete | 删除存储文件、file 记录、album_photo 关联与 photo 记录 |
| 清空回收站 | /photo/clear | 按设置决定是否同步删除存储文件 |

- 所有修改类接口均以 `AND photo.userId = 当前用户` 为条件，**他人照片无法被修改**。
- 前端瀑布流的选择抽屉（删除/加入相册/移出相册）会把选中照片过滤为本人照片；当选中项不含本人照片时不显示管理按钮，非本人卡片也不显示收藏按钮。

### 4.5 相册管理流程

```
创建相册（/album/add）→ 设置名称与可见性（公开/私密）
        │
        ▼
相册列表（/album/list，仅当前用户自己的相册，60s 缓存）
        │
        ▼
进入相册 → 相册照片页（/photo/list + albumId，仅当前用户照片）
        │
        ▼
管理操作：重命名 / 置顶 / 改可见性 / 删除相册 / 加入照片 / 移出照片
```

**可见性规则（本次调整后）**：

- 公开相册：照片在照片墙正常显示。
- 私密相册：照片**不会出现在任何人的照片墙**上（子查询排除所有用户私密相册中的照片）。
- 照片加入私密相册后，从照片墙「隐藏」；移出或改为公开相册后重新显示。
- 相册本身仍是个人空间：其他用户看不到你的相册列表，也无法打开相册详情。

### 4.6 收藏流程

```
照片墙卡片点击红心（仅本人照片可操作）
        │
        ▼
/photo/favorite 更新 photo.favorite 标记（1 未收藏 / 2 已收藏）
        │
        ▼
「收藏」页按 favorite=已收藏 查询照片墙 → 展示全部被收藏的公开照片
```

> 说明：收藏标记存储在 photo 记录上（全局字段）。照片墙公开化后，「收藏」页展示的是所有用户收藏过的公开照片，收藏页同样隐藏私密相册照片。

### 4.7 回收站与定时清理流程

```
照片墙批量「删除」→ 照片进入回收站（status=删除）
        │
        ▼
回收站虚拟相册（/album/trash，统计本人已回收照片与最新封面）
        │
        ▼
回收站照片页（/photo/list + status=删除，仍按 userId 过滤，仅本人可见）
        │
        ├─ 恢复 → /photo/restore
        ├─ 彻底删除 → /photo/delete
        └─ 清空 → /photo/clear
        │
        ▼
定时任务（node-cron + Vercel Cron）→ /photo/clearExpired
  按设置「保留 N 天」清理过期回收站照片（每批 100 条循环处理）
```

- 回收站列表**始终保持个人隔离**：即使照片墙公开化，回收站也只显示当前用户的已删除照片。

### 4.8 用户管理流程（管理员）

```
管理员进入「用户」页（/user/list）→ 用户数据表
        │
        ├─ 新增用户（用户名 + 密码 + 类型）
        ├─ 编辑（重置密码、切换类型）
        ├─ 启用 / 禁用
        └─ 删除用户 → 级联删除其照片、文件、相册与回收站内容（/user/delete）
        │
        ▼
用户维度统计：照片总数、已用容量（/user/list 聚合 photo 表）
```

### 4.9 存储管理流程

```
「存储」页（/storage/list）→ 存储配置数据表
        │
        ├─ 新增存储：类型（本地 / S3 兼容 / Vercel Blob）+ 桶、区域、端点、密钥、访问域名
        ├─ 编辑 / 启用 / 禁用 / 置顶
        └─ 删除存储
        │
        ▼
上传照片时按存储配置写入；媒体读取按 photo.storageId 找到对应存储
```

- 存储用量看板：按存储聚合照片总数与已用容量。
- 上传弹窗内可切换目标存储。

### 4.10 系统设置流程

| 设置项 | 说明 |
|--------|------|
| 照片去重 | 开启后按文件 SHA-1 跳过本人重复上传（/photo/exists） |
| 同步删除 | 开启后删除照片时同步删除存储文件 |
| 定时清理 | 开启后按「保留 N 天」定时清空回收站 |

---

## 5. 数据模型（核心表）

| 表 | 关键字段 | 说明 |
|----|----------|------|
| photo | photo_id、name、checksum、type、size、width、height、taken_time、create_time、recycle_time、user_id、status（1正常/2回收）、favorite（1/2）、storage_id | 照片主记录；`user_id` 标记所有者 |
| file | file_id、photo_id、key、type（原图/预览/缩略图）、file_type、size | 照片文件索引 |
| album | album_id、name、visibility（0公开/1私密）、user_id、sort、create_time、update_time | 相册（个人数据） |
| album_photo | id、album_id、photo_id | 相册-照片多对多关联 |
| user | user_id、username、password、salt、avatar、type（admin/user/demo）、status | 用户 |
| storage | storage_id、name、type、domain、bucket、region、endpoint、access_key、secret_key、status、sort | 存储配置 |

---

## 6. 可见性矩阵（本次变更后）

| 场景 | 本人照片 | 他人照片 |
|------|----------|----------|
| 照片墙列表 / 日期统计 | ✅ 可见 | ✅ 可见（私密相册中的除外） |
| 媒体文件（原图/预览/缩略图） | ✅ 可查看 | ✅ 可查看 |
| 收藏页 | ✅ 展示本人已收藏 | ✅ 展示他人已收藏的公开照片 |
| 回收站 | ✅ 仅本人 | ❌ 不可见 |
| 相册 | ✅ 仅本人相册 | ❌ 不可见 |
| 收藏 / 回收 / 删除 / 加相册 | ✅ 可操作 | ❌ 前端隐藏按钮 + 后端拒绝 |

---

## 7. 照片相关接口清单

| 接口 | 方法 | 用途 | 可见性策略 |
|------|------|------|------------|
| /photo/list | POST | 分页照片列表 | 墙：所有用户公开照片；回收站：仅本人 |
| /photo/takenDateList | POST | 拍摄日期统计 | 同上 |
| /photo/createUrl | POST | 生成直传预签名 URL | 登录即可 |
| /photo/add | POST | 上传登记照片 | 登录即可，写入本人 user_id |
| /photo/exists | POST | 去重检查 | 仅查本人照片 |
| /photo/recycle / restore / delete / clear | POST | 回收站操作 | 仅本人照片 |
| /photo/favorite | POST | 收藏切换 | 仅本人照片 |
| /media/{key} | GET | 读取媒体文件 | 登录用户均可读 |

---

## 8. 本次变更涉及文件

| 文件 | 变更内容 |
|------|----------|
| `src/server/service/photo-service.ts` | 照片墙列表与日期统计不再按 userId 过滤；私密相册排除范围扩展到所有用户；相册/回收站场景保持本人限定 |
| `src/server/hono/media.ts` | 媒体文件读取移除 userId 限制，登录用户均可访问 |
| `src/components/photo/photo-upload-dialog.tsx` | 上传弹窗顶部新增「默认为公开」醒目提示 |
| `src/components/photo/photo-card.tsx` | 非本人照片不渲染收藏按钮 |
| `src/components/photo/photo-masonry.tsx` | 批量管理操作过滤为本人照片；选中无本人照片时隐藏管理按钮 |
| `src/components/photo/photo-selection-drawer.tsx` | 删除按钮改为可选渲染 |
| `locales/web/zh.json` / `en.json` | 新增 `photos.upload.publicNotice` 提示文案 |
